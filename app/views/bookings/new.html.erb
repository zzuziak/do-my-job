<h1>Booking request</h1>

<%= simple_form_for([@job, @booking], url: job_bookings_path) do |f| %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.label :message %>
  <%= f.text_field :message %>

  <%= f.submit %>
<% end %>
